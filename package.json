{
  "name": "ai-finance-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "ml": "cd ml-service && uvicorn app.main:app --reload",
    "dev": "concurrently \"npm run server\" \"npm run ml\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "react-icons": "^5.7.0",
    "react-router-dom": "^7.18.1"
  }
}
